# Default values for golang.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: 012569964433.dkr.ecr.eu-west-1.amazonaws.com/go-app:b5
  tag: 44ca506
  pullPolicy: IfNotPresent
service:
  name: golang
  type: ClusterIP
  externalPort: 80
  internalPort: 8080
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
ingress:
  enabled: true
